
import React from 'react';

// This component is no longer used in the new disk-based, always-saved model.
// It's kept as an empty component to prevent import errors.
const ConfirmCloseModal: React.FC = () => {
  return null;
};

export default ConfirmCloseModal;
